VITE_SERVER_URL=http://localhost:4000
# Production example (set as an environment variable in the Vercel project,
# not in this file — Vite only reads real .env files, this one is just a template):
# VITE_SERVER_URL=https://inksy-production.up.railway.app
